# 项目快速启动指南

> 内蒙古农畜产品品牌营销AI赋能云平台 - 开发环境快速搭建

**开发者**: dctx479
**更新日期**: [项目完成日期]
**版本**: 0.1.0 MVP

---

## 前置要求

请确保已安装以下工具：

- ✅ Node.js 18+ ([下载地址](https://nodejs.org/))
- ✅ Python 3.11+ ([下载地址](https://www.python.org/))
- ✅ Docker 20+ ([下载地址](https://www.docker.com/))
- ✅ Git ([下载地址](https://git-scm.com/))
- ✅ pnpm (推荐) 或 npm

## 快速开始（5步启动）

### 步骤1: 启动数据库服务 (2分钟)

```bash
# 进入Docker目录
cd deploy/docker

# 启动MySQL和Redis
docker-compose up -d

# 验证容器运行状态
docker-compose ps

# 预期输出：
# agri-mysql  Up 0.0.0.0:3306->3306/tcp
# agri-redis  Up 0.0.0.0:6379->6379/tcp
```

**数据库连接信息**:
- MySQL: `localhost:3306`，用户名: `root`，密码: `root123`
- Redis: `localhost:6379`

### 步骤2: 启动后端服务 (3分钟)

```bash
# 进入后端目录
cd backend

# 创建Python虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
.\venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 复制环境变量文件
cp .env.example .env

# 启动后端
uvicorn app.main:app --reload

# 预期输出：
# INFO:     Uvicorn running on http://127.0.0.1:8000
# INFO:     Application startup complete
```

**访问后端API文档**:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 步骤3: 启动前端服务 (3分钟)

```bash
# 新开一个终端，进入前端目录
cd frontend

# 安装依赖
pnpm install
# 或使用npm: npm install

# 启动前端开发服务器
pnpm dev
# 或: npm run dev

# 预期输出：
# VITE v5.0.11 ready in 500 ms
# ➜  Local:   http://localhost:5173/
```

**访问前端页面**: http://localhost:5173

### 步骤4: 验证端到端连通性 (1分钟)

1. 打开浏览器访问 http://localhost:5173
2. 在首页点击 **"检查API连接"** 按钮
3. 如果显示 **"✓ 后端API连接正常"**，说明环境搭建成功！

### 步骤5: 开始开发 🎉

恭喜！你的开发环境已经就绪，可以开始编码了。

---

## 项目结构

```
AI赋能云平台/
├── backend/                # 后端（FastAPI + Python）
│   ├── app/
│   │   ├── main.py        # FastAPI入口
│   │   ├── core/          # 核心配置
│   │   ├── api/           # API路由（TODO）
│   │   ├── models/        # 数据模型（TODO）
│   │   └── services/      # 业务逻辑（TODO）
│   ├── requirements.txt   # Python依赖
│   └── .env.example       # 环境变量模板
│
├── frontend/              # 前端（Vue 3 + TypeScript）
│   ├── src/
│   │   ├── main.ts       # 应用入口
│   │   ├── App.vue       # 根组件
│   │   ├── router/       # 路由
│   │   ├── views/        # 页面
│   │   │   ├── Home.vue  # 首页
│   │   │   └── Login.vue # 登录页（TODO）
│   │   ├── components/   # 组件（TODO）
│   │   └── utils/        # 工具（TODO）
│   ├── package.json      # npm配置
│   └── vite.config.ts    # Vite配置
│
└── deploy/docker/         # Docker配置
    ├── docker-compose.yml # 容器编排
    └── init/mysql/        # 数据库初始化脚本
        └── 01-init.sql   # MySQL初始化
```

---

## 开发工作流

### 日常开发流程

1. **启动服务**:
   ```bash
   # 终端1: 数据库
   cd deploy/docker && docker-compose up -d

   # 终端2: 后端
   cd backend && uvicorn app.main:app --reload

   # 终端3: 前端
   cd frontend && pnpm dev
   ```

2. **编写代码**: 在 `backend/app/` 或 `frontend/src/` 中开发

3. **测试功能**: 访问 http://localhost:5173 测试

4. **提交代码**:
   ```bash
   git add .
   git commit -m "feat: 添加xxx功能"
   git push
   ```

### 常用命令

**后端**:
```bash
# 安装新依赖
pip install package-name
pip freeze > requirements.txt

# 运行测试
pytest

# 数据库迁移（TODO: 配置alembic后）
alembic revision --autogenerate -m "描述"
alembic upgrade head
```

**前端**:
```bash
# 安装新依赖
pnpm add package-name

# 类型检查
pnpm run build

# 构建生产版本
pnpm run build
```

**Docker**:
```bash
# 停止所有容器
docker-compose down

# 查看日志
docker-compose logs -f mysql
docker-compose logs -f redis

# 重启容器
docker-compose restart
```

---

## 常见问题

### Q1: 后端启动失败，提示"ModuleNotFoundError"

**解决方案**:
```bash
# 确保虚拟环境已激活
.\venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 重新安装依赖
pip install -r requirements.txt
```

### Q2: 前端启动失败，提示"Cannot find module"

**解决方案**:
```bash
# 删除node_modules重新安装
rm -rf node_modules package-lock.json
pnpm install
```

### Q3: Docker容器无法启动

**解决方案**:
```bash
# 检查端口是否被占用
netstat -ano | findstr "3306"  # Windows
lsof -i :3306  # Linux/Mac

# 停止占用端口的服务或修改docker-compose.yml中的端口映射
```

### Q4: 前端无法访问后端API

**解决方案**:
1. 确认后端已启动在 `http://localhost:8000`
2. 检查 `frontend/vite.config.ts` 中的代理配置
3. 检查浏览器控制台是否有CORS错误
4. 确认后端 `app/main.py` 中的CORS配置包含前端地址

### Q5: 数据库连接失败

**解决方案**:
1. 确认Docker容器正在运行: `docker-compose ps`
2. 检查 `backend/.env` 中的数据库连接字符串
3. 使用MySQL客户端测试连接: `mysql -h localhost -u root -proot123`

---

## 下一步

现在你已经成功搭建了开发环境，可以参考以下文档继续开发：

1. **查看任务列表**: `docs/planning/项目任务清单.md`
2. **API设计规范**: `docs/api/api-design-spec.md`
3. **数据库设计**: `docs/design/database-design.md`
4. **项目分析报告**: `docs/planning/项目分析报告.md`

### MVP第一个Sprint（2周）

参考 **Week 1-2任务**:
1. 创建用户认证API（注册、登录）
2. 创建前端登录页面
3. 实现JWT Token机制
4. 完成前后端联调

---

## 获取帮助

- **开发者**: dctx479
- **邮箱**: b150w4942@163.com
- **项目文档**: `docs/PROJECT-GUIDE.md`

---

**祝开发顺利！🚀**
